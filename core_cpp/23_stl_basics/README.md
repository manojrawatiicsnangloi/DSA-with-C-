# C++ Standard Template Library (STL) Topics ✅
## 1. Containers
### 1.1 Sequence Containers

1. `vector`
2. `deque`
3. `list`
4. `array`
5. `forward_list`

### 1.2 Associative Containers

6. `set`
7. `multiset`
8. `map`
9. `multimap`

### 1.3 Unordered Containers

10. `unordered_set`
11. `unordered_multiset`
12. `unordered_map`
13. `unordered_multimap`

### 1.4 Container Adapters

14. `stack`
15. `queue`
16. `priority_queue`

---

## 2. Iterators

17. `begin()`, `end()`
18. `rbegin()`, `rend()`
19. `cbegin()`, `cend()`
20. `reverse_iterator`
21. `iterator_traits`

---

## 3. Algorithms

22. `sort()`
23. `reverse()`
24. `accumulate()`
25. `count()`
26. `find()`
27. `binary_search()`
28. `lower_bound()`, `upper_bound()`
29. `max_element()`, `min_element()`
30. `next_permutation()`, `prev_permutation()`
31. `for_each()`
32. `fill()`
33. `swap()`

---

## 4. Utility Components

34. `pair`
35. `tuple`
36. `make_pair()`, `tie()`
37. `std::move()`
38. `std::swap()`

---

## 5. Additional Concepts

39. `function` (function wrapper)
40. Lambda expressions
41. Functors / Function Objects
42. Comparators and custom sorting
43. STL complexity rules (O(1), O(log n), O(n))
