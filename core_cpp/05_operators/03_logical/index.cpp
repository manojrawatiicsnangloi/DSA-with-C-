#include <iostream>
using namespace std;

int main() {

    int a = 10;
    int b = 5;
 
    cout << "\nLogical Operators\n";
    bool p = true;
    bool q = false;

    cout << "p && q : " << (p && q) << endl;
    cout << "p || q : " << (p || q) << endl;
    cout << "!p     : " << (!p) << endl;
    return 0;
}